name: Docker Image Production
on:
  push:
    branches: ['main']

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Set up Docker Buildx
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # Build and push Docker image with BuildX
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./docker/prod.Dockerfile
          push: true
          platforms: linux/amd64,linux/arm64
          build-args: |
            API_URL=${{ secrets.API_URL }}
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/nisit-deeden-sveltekit:latest
  deploy:
    needs: build-and-push
    runs-on: self-hosted # self-hosted runner in your machine
    environment: production

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: show kube context
        run: |
          kubectl config current-context
          kubectl get nodes

      - name: Setup env
        run: |
          echo "${{ secrets.SVELTEKIT_ENV }}" > .env

      - name: Create k8s secret
        run: |
          kubectl create secret generic sveltekit-env \
            --from-env-file=.env \
            --namespace=default \
            --dry-run=client -o yaml | kubectl apply -f -

      - name: Deploy to Kubernetes
        env:
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
        shell: bash
        run: |
          envsubst < k8s/deployment.yml | kubectl apply -f -
          kubectl apply -f k8s/service.yml

          kubectl rollout restart deployment/nisit-deeden-sveltekit
          kubectl rollout status deployment/nisit-deeden-sveltekit

      # - name: Cleanup env
      #   run: rm -f .env
